
version: 2

models:
  - name: valerian
    description: "test to try"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null